#@author:Alexander  Inversion.py
class Inversion:
  #este es el método constructor que inicializa
  #a los atributos de una clase
  def __init__(self, valor1, valor2, valor3, valor4):
    self.saldo = valor1
    self.rendimiento = valor2
    self.tiempo = valor3
    self.empresa = valor4
  def depositar(self, cantidad):
    self.saldo=self.saldo + cantidad
  def retirar(self, cantidad):
    self.saldo=self.saldo - cantidad
  #metodo que calcula la ganancia final de la inversion
  def calcular(self):
      self.capital = self.saldo* self.rendimiento * self.tiempo
  def mostrar(self) :
    print(self.empresa)

  #Menu.py
  #from Inversion import
class Menu:
  pass
print("Probando la inversion")

inversion1 = Inversion(3000, 4.5, 24, "Apple")
print(inversion1.saldo)
print(inversion1.rendimiento)
print(inversion1.tiempo, "meses")
print(inversion1.empresa)

inversion1.depositar(1500)
print("Probando el deposito")
print(inversion1.saldo)

inversion1.retirar(500)
print("Probando el retiro")
print(inversion1.saldo)

inversion1.calcular()
print("Probando el calculo")
print(inversion1.capital)
